#!/bin/sh
# Evita que Cursor quede como coautor en commits de este repo.
case "$2" in
  merge|squash) exit 0 ;;
esac
if [ -f "$1" ]; then
  grep -vi '^Co-authored-by: Cursor' "$1" | grep -vi '^Made-with: Cursor$' > "${1}.tmp" && mv "${1}.tmp" "$1"
fi
